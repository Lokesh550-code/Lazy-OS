const StartUpMenuApplication = ({name}) => {
  return (
    <h1 className="border-l text-center cursor-pointer hover:bg-[#1b1a1a] rounded-r transition select-none">
        {name}
    </h1>
  );
};

export default StartUpMenuApplication;
